<?php

namespace Emagia\Tests;

use Emagia\Character;
use Emagia\Battle;
use Emagia\Player;
use Emagia\Stat;
use PHPUnit\Framework\TestCase;


class GameTest extends TestCase
{
    /**
     * @test
     */
    public function a_game_starts()
    {
        // $health = new Stat("Health");
        // $health->setBoundaries(60, 70);
        // $orderus = new Character("Orderus");
        // $orderus->addStat($health);
        // $player1 = new Player("John");
        // $player1->setCharacter($orderus);


        // $beast = new Character("Beast");
        // $beast->addStat($health);
        // $player2 = new Player("CPU");
        // $player2->setCharacter($beast);

        // $game = new Battle();
        // $game->addPlayer($player1);
        // $game->addPlayer($player2);

        // $winner = $game->start();

        // $this->assertEquals($player1, $winner);

    }
}
